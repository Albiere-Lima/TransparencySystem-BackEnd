version: '3.8'

services:
  db:
    image: postgres:15
    container_name: db_transparenciaCA
    environment:
      POSTGRES_DB: transparencia_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin_password
    ports:
      - "5432:5432"